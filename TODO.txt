======================================================================================
/////////////////////////////// DONE \\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\
======================================================================================

1. KVCAN and maybe other protocols: is message to be sent is longer than 8 bytes and no TP protocol was selected then the message is wrong, reject at validation ?
-> DONE

2.) check if the other protocols shall use the same dumping mechanism as KCAN
-> DONE

3) move testers inside sources, create cmake for every component, use also dumper to show the received data
-> DONE

5) show data with longer dumpsizes also in the normal view not only in the full dump view
-> DONE

4) allow to select /copy the content in the core dump window as it is
-> DONE


5) remove ExpandAll button of commdumpviewer
-> DONE

6) arrays shall accept numeric values as index
array [= ...
$array.0
if this validation fails, stop execution
Array [ cfgs ] index 70 out of range (size= 5 )
-> DONE

8) allow to build structures either from individual bytes or absolute
CONVERT  56:3:22 12:6:43 | 64:00
byte.index field.offset field.size field.value

8.5.2=12 | 16 FF => 16 to how many bytes to generate, FF default value for the unused bytes
Fail if the number of bits allocated and the
-> DONE

9) Allow spaces inside the H"" declarations (remove them at before processing)
-> DONE

15) HEX_FLOAT_LE HEX_FLOAT_BE HEX_DOUBLE_LE HEX_DOUBLE_BE 
TRUE FALSE are not highlighted in the scriptgui frontend
-> DONE

13) Extend plugins with a command to send cyclical messages..

array [= \
id1 val1 time1, \
id2 val2 time2, \
id3 val3 time3, \
...
idN valN timeN, \

KCAN.CYCLIC $array &
which starts a timer with the timeout computed as the common denominator of {time1, time2, ... timeN} and sends the corresponding value at the specified interval either once if '&'' is missing or threaded if '&'' is placed after the command

This command shall be implemented for all plugins, therefore it has to be implemented in a common section and reused as generic_send_cyclic in all plugins with a minimal work.

16) CYCLIC command takes as parameters $array which is automatically expanded.
So practically it receives and must handle a list of: id1 val1 time1, id2 val2 time2,... idN valN timeN as argument, so it can simply split it by comma and gets groups of: id val time space separated which are then splitted by space, the components are extracted and used to initialize a structure which is then pushed to a vector.
The execution function simply loop through the vector check is the timeout occurs and call a send for that data.
The first argument ID can have different meanings depending of the plugin, is CAN ID for CAN related plugins, PORT for UART, Address:port for ethernet related ones or can be absent.
Maybe if it makes sense we can use inversed order for members like: {time val [id]} where the id is optional (can be absent ) in some cases (e.g ethernet or UART related when all the messages are sent to the same port or address)


======================================================================================
////////////////////////////// TO DO \\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\\
======================================================================================

10) Extend UART plugin with a TERMINAL command which popup the shell and accept to communicate with any devices via shell

11) xtra_args shall support / and | inside the brackets

12) MQTT plugin/driver

Provide a separation between Protocol side, driver side and plugin side.
Put the MQTT data handling into the protocol side which is driver agnostic
The MQTT Plugin will connect the protocol with the driver and based on the command (SUBSCRIBE/UNSUBSCRIBE, PING, PUBLISH) will putt the corresponding data into the send buffer and call the driver to send it.

RECEIVE must be handled as MQTT.CMD < and MQTT.CMD < & when threaded16) CYCLIC command takes as parameters $array which is automatically expanded.
So practically it receives and must handle a list of: id1 val1 time1, id2 val2 time2,... idN valN timeN as argument, so it can simply split it by comma and gets groups of: id val time space separated which are then splitted by space, the components are extracted and used to initialize a structure which is then pushed to a vector.
The execution function simply loop through the vector check is the timeout occurs and call a send for that data.
The first argument ID can have different meanings depending of the plugin, is CAN ID for CAN related plugins, PORT for UART, Address:port for ethernet related ones or can be absent.
Maybe if it makes sense we can use inversed order for members like: {time val [id]} where the id is optional (can be absent ) in some cases (e.g ethernet or UART related when all the messages are sent to the same port or address)

All other commands must be integrated under MQTT.CMD > as parameters
From "PLUGIN.CMD > arg" perspective everything after > is a simple string argument (arg). The user shall avoid the pipe character '|' inside that string or border the string with brackets "").
An intermediary layer shall decompose that string provided as argument to the command  extract the MQTT specific command (SUBSCRIBE/UNSUBSCRIBE, PING, PUBLISH, etc.) and the arguments, validate them and then based on command type call the specific callback associated to that command with the arguments that follow after the command.

Actually the CMD is mapped directly to publish, the change shall make PUBLISH just another command under the other

Examples:

MQTT.CMD > SUBSCRIBE <topic> [qos] |
MQTT.CMD > UNSUBSCRIBE <topic>
MQTT.CMD > PING
MQTT.CMD > PUBLISH <topic>

The MQTT.CMD doesn't handle the xtra_args; if a command shall also receive something then use the standard syntax:

MQTT.CMD > send_side | receive_side

e.g.

MQTT.CMD > PUBLISH OPEN actuators/valve3/cmd | PUBACK

where:

PUBLISH - is the MQTT command extracted by the interḿediate layer which decides to callback to be used (here the publish_cb(..))
OPEN actuators/valve3/cmd - is the argument for the command
PUBACK is the message expected to be received (it has to be decoded to the exact real bytes to be received by that intermediary layer) and being on the receive_side of the CMD > fits natuaral in the implementation.

---
MQTT plugin have to use the same pattern for CMD and SCRIPT as the most of the other plugins.
But with the difference that it must not use directly the real comm driver but instead to wrap it under another virtual driver with the same interface as the real driver (therefore inherited from the same ICommDriver.hpp) which first process the content of the argument (SUBSCRIBE UNSUBSCRIBE PUBLISH etc.) and after it determine the data to be sent to the broker (specific for every command) then call the real comm driver (ethernet) and send it.
---
Check if we can keep the connection open for a MQTT plugin over it's entire script lifecycle via a setting in INIFILE/CONFIG
This means to do not open/close it with every CMD but open it with the first command and keep it open until EOScript
---


14) the uscript interpreter must be loaded automatically on the front-end path and not stored/reloaded in the QT persistent storage.
When the path is changed at runtime the new path shall be used until the application is closen.
In conclusion when the RUN button is pressed the uscript path must be read/rebuilt every time from the edit box in the window header
But the script/ini-file shall be stored / reloaded as in the current implementation from the QT storage



17) Implement a terminal command for most of the plugins (UART, TCPIP, UDP, CAN(?)) where the user can prepare a string to be sent using plugin specific syntax and protocol.
The edit shall be handled by the terminal and when enter is pressed the resulted string must be passed to the transport protocol. (check if it makes sense as the shell plugin already handle this)


:STR :NUM :VER :BOOL highlight must accept spaces between : and NUM but must be forced to have at least one space in front of : in order to be accepted

18) Add websocket plugin + loopback tester
19) Add modbus plugin + loopback tester
20) For delays inside the REPEAT loops use an absolute time reference ?
If delay in REPEAT then the timer is started when REPEAT loop starts and every delay is compensated to this time ?


----- NEXT -------------
21) COMM dump : add option of Data:32 additional to 8 and 16
22) Check if possible to print also the instance number of the plugin in commdump
23) MQTT return topic value as string not as hexdump
24) move all script internals to log severity WERBOSE (create it)
25) move the execution time to severity FIXED

26) check why in scriptgui sometimes seem to pop-up a window which is immediatelly closed / disable this
27) Add a time anchor in REPEAT loops so the delay to be relative to that reference time and to cumulated
28) Add option for all loopback to be also simple listeners with dumping of received data or simple senders (increment a counter and send it at a given interval of time specified as cli parameter). Also add the timestamp for messages