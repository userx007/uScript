[SHARED]
FAULT_TOLERANT          = FALSE
PRIVILEGED              = FALSE
ARTEFACTS_PATH          = .
UART_PORT               = /dev/tnt0
BAUDRATE                = 115200
READ_TIMEOUT            = 3000
WRITE_TIMEOUT           = 3000
READ_BUF_SIZE           = 1024
READ_BUF_TIMEOUT        = 3000
SCRIPT_DELAY            = 0


[COMMON]
LOG_SEVERITY_CONSOLE    = 0
LOG_SEVERITY_FILE       = 0
LOG_INCLUDE_DATE        = FALSE
LOG_CONSOLE_COLORED     = TRUE
LOG_FILE_ENABLED        = FALSE


[SCRIPT]
CMD_EXEC_DELAY          = 0


[UTILS]
PRIVILEGED              = ${SHARED:PRIVILEGED}


[SHELL]
PRIVILEGED              = TRUE


[UART]
PRIVILEGED              = FALSE
ARTEFACTS_PATH          = ${SHARED:ARTEFACTS_PATH}
UART_PORT               = ${SHARED:UART_PORT}
BAUDRATE                = ${SHARED:BAUDRATE}
READ_TIMEOUT            = ${SHARED:READ_TIMEOUT}
WRITE_TIMEOUT           = ${SHARED:WRITE_TIMEOUT}
READ_BUF_SIZE           = ${SHARED:READ_BUF_SIZE}
READ_BUF_TIMEOUT        = ${SHARED:READ_BUF_TIMEOUT}


[CP2112]
ARTEFACTS_PATH          = ${SHARED:ARTEFACTS_PATH}
DEVICE_INDEX            = 0
I2C_CLOCK               = 100
I2C_ADDRESS             = 0x27
READ_TIMEOUT            = ${SHARED:READ_TIMEOUT}
SCRIPT_DELAY            = ${SHARED:SCRIPT_DELAY}


[BUSPIRATE]
ARTEFACTS_PATH          = ${SHARED:ARTEFACTS_PATH}
UART_PORT               = ${SHARED:UART_PORT}
BAUDRATE                = ${SHARED:BAUDRATE}
READ_TIMEOUT            = ${SHARED:READ_TIMEOUT}
WRITE_TIMEOUT           = ${SHARED:WRITE_TIMEOUT}
READ_BUF_SIZE           = ${SHARED:READ_BUF_SIZE}
READ_BUF_TIMEOUT        = ${SHARED:READ_BUF_TIMEOUT}
SCRIPT_DELAY            = ${SHARED:SCRIPT_DELAY}


[HYDRABUS]
ARTEFACTS_PATH          = ${SHARED:ARTEFACTS_PATH}
UART_PORT               = ${SHARED:UART_PORT}
BAUDRATE                = ${SHARED:BAUDRATE}
READ_TIMEOUT            = ${SHARED:READ_TIMEOUT}
WRITE_TIMEOUT           = ${SHARED:WRITE_TIMEOUT}
READ_BUF_SIZE           = ${SHARED:READ_BUF_SIZE}
READ_BUF_TIMEOUT        = ${SHARED:READ_BUF_TIMEOUT}
SCRIPT_DELAY            = ${SHARED:SCRIPT_DELAY}


[CORE]

[FT2232]

[FT232H]

[FT245]

[FT4232]



[UARTMON]

