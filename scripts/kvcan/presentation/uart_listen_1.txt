LOAD_PLUGIN UART

UART.CONFIG p=/dev/tnt0 b=115200 r=2000000 w=2000 s=1024 
prvVal ?= $newVal
newVal ?= UART.CMD < &

r ?= REPEAT loop UNTIL FALSE
    IF EVAL $newVal == $prvVal | STR GOTO skip
        PRINT GOT: $newVal
    LABEL skip
    prvVal ?= $newVal
    DELAY 100 ms 
END_REPEAT loop
